# Copy to keystore.properties in the repository root. Never commit the real file.
# Keep the existing signing key to preserve upgrade compatibility.
storeFile=app/workspace-release.jks
storePassword=REPLACE_LOCALLY
keyAlias=workspace_key
keyPassword=REPLACE_LOCALLY
